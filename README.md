# LinuxGUI

## 最もカンタンなインストール方法（Debian / Ubuntu）
以下の依存パッケージが必要です。
- build-essential
- libgtk-3-dev
- pkg-config

セットアップ手順例:
```bash
git clone https://github.com/caster0469/LinuxGUI.git
cd LinuxGUI
chmod +x install.sh
./install.sh
```

インストール完了後は `hybrid-desktop` コマンド、またはデスクトップメニューから起動できます。
